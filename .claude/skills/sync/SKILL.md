---
name: sync
description: Install or sync all project dependencies (including dev extras) via uv.
---

Run:

```bash
uv sync --extra dev
```

Report whether the sync succeeded and any notable version changes.
