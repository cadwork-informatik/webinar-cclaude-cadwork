---
name: run-app
description: Start the FastAPI app with uvicorn on port 8000
---

Run this command to start the app:

```
uv run uvicorn app:app --reload --port 8000
```

Then the user can open http://localhost:8000.
